linters:
  disable-all: true
  enable:
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - unused
    - gosimple
    - gofmt

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
